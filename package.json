{
  "name": "cyber-stack",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "postinstall": "npm install --prefix backend",
    "start": "npm --prefix backend start"
  },
  "dependencies": {
    "xlsx": "^0.18.5"
  }
}
